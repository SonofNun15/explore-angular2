import { describe, it } from 'angular2/testing';
import { HeroCreatorComponent } from './heroCreator';

describe('heroCreator', (): void => {
   it('true should be false', (): void => {
       expect(true).toEqual(false);
   }) ;
});